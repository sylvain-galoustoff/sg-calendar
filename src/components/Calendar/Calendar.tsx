import DateSelect from "./DateSelect";
import Grid from "./Grid";

function Calendar() {
  return (
    <div id="calendar">
      <DateSelect />
      <Grid />
    </div>
  );
}

export default Calendar;
